<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Objetos personalizados (incluye fields y record types) -->
    <types>
        <members>*</members>
        <name>CustomObject</name>
    </types>
    <!-- Objeto Account con campos personalizados -->
    <types>
        <members>Account.*</members>
        <name>CustomField</name>
    </types>

    <!-- Objetos estándar para fields y layouts -->
    <types>
        <members>*</members>
        <name>CustomField</name>
    </types>

    <!-- Record Types -->
    <types>
        <members>*</members>
        <name>RecordType</name>
    </types>

    <!-- Layouts -->
    <types>
        <members>*</members>
        <name>Layout</name>
    </types>

    <version>58.0</version>
</Package>
